name: iqvia_kpi
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  gitlab:
    git:
      url: git@github.com:beshkarev/gitlab-dart.git
  freezed_annotation: ^2.0.1
  flutter_bloc: ^8.0.1
  json_annotation: ^4.5.0
  shimmer: ^2.0.0
  hive: ^2.2.2
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^5.0.2
  get_it: ^7.2.0
  flutter_svg: ^1.1.1+1
  popover: ^0.2.6+3
  expandable: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^2.1.10
  freezed: ^2.0.2
  json_serializable: ^6.1.4
  hive_generator: ^1.1.3

flutter:
  uses-material-design: true

  assets:
    - assets/assets/
